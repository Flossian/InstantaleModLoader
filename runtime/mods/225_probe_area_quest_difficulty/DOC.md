# `225_probe_area_quest_difficulty`

街を初めて訪ねたとき、その街の依頼の難易度を誰がどう決めているかを録る。`settlement_quest_generator` に渡る `quest_difficulties` の実値と呼び出し元とそのローカル変数、stat↔難易度の変換、街が作られる経路の順番と前後の `level_of_detail`、到着の移動の間の `random.*`。`133_ui_area_difficulty` が訪問前の街にも帯を出すための材料（VERIFICATION.md §3.49 #5）
