# `101_fix_npc_employ_price`: NPC を雇うと落ちるのを直す

特定の NPC を雇おうとすると落ちる `KeyError: 80` を、
ゲーム自身が持っている「値を決められた範囲に収める処理」を通して防ぐ。

> **main_024 で本体が取り込んだため無効化**。
> デバッグモードのときだけ読み込まれます（README「デバッグモード」）。
> ゲームが自分で値を丸めるようになったので、この MOD は要りません。
