# `908_npc_carryover`: 別の世界のNPCを新しい世界へ連れてくる

配らない理由: 実機で確かめていない。
書けているのは中身（`carryover.py` / `tool.py` / `npc_carryover.py`）と
ゲーム抜きの検証（`tools/tests/test_wip_npc_carryover.py`。84項目）まで。
実機で通った（§9.2。4体を持ち込んで、セーブに残るところまで確認済み）。
残るのは `403_` の社会関係・`120_` の部分一致・装備持ちの3つ（§9.4）。
どれも「まだその場面を引いていない」もので、直すところは無い。

```
908_npc_carryover/
├─ carryover.py       セーブの復号・zip・予約。画面とフックの両方が使う
├─ tool.py            ゲームの外の画面（設定画面から開く）
└─ npc_carryover.py   ゲームの中の取り込み（ロード時）
224_probe_npc_carryover/   ロードのどの地点から入れられるかの計測（§9 の 1）
```

リリース時に `docs/` へ戻す節（TECH.md §2.6）:

| この文書の節 | 戻す先 |
| --- | --- |
| §1〜§2（遊び方・要件） | `docs/MODS.md` |
| §4（ローダ基盤） | `docs/TECH.md` §3（ローダの語彙） |
| §9（確かめたこと・未確認と確認手順） | `docs/VERIFICATION.md` |

## 1. 何をする機能か（要件）

遊び終えた世界のNPCをエクスポートしておき、別の世界にインポートして登場させる。
再会したときに同じ人物として話せるよう、見た目・人物像・好感度に加えて、
`311_` / `403_` が `state\` に持つ記憶も引き継げる。

決定事項（2026-08-28 の要件確認）:

| 論点 | 決定 |
| --- | --- |
| 実現形態 | ゲーム内MOD（ModLoader の新規 MOD） |
| 取り込み元 | 他世界のセーブ（`saves\<世界名>\savedata.json`）を直接読む。対象のセーブとNPCは GUI で指定 |
| エクスポートの持ち方 | 指定したNPCを世界ごとに zip 化して `state\` に保持。一覧のチェックで複数選び、一括でエクスポートできる |
| インポートの指定 | どの zip をどの世界に置くかも GUI で予約する。置き先は世界名で指定（`saves\` に在る世界から選ぶ） |
| GUI の開き方 | ローダの設定画面（`tools/gui.py`）から開く。MOD が独自 GUI を持てる基盤をローダに足す（§4） |
| 配置の発動 | 対象世界のロード時に自動 |
| 配置先 | ダンジョン以外のランダムな街の、ギルドか宿（半々）。旅の者がこの土地に流れて来た、という位置づけで冒険者として登録する |
| 同名NPCが既に居たら | 予約の時点で GUI が置き先のセーブと突き合わせて表示する（ゲームを起動する前に分かれば手戻りが無い）。予約後に生まれた衝突だけゲーム内で見送り、メッセージで通知する（黙って出てこないとバグに見えるため） |
| 記憶（`311_` / `403_`）の引き継ぎ | 設定で選べる（§7・§8） |

## 2. 全体の構成

部品は4つ。GUI はゲームの外（別プロセス）、取り込みはゲームの中で行う。

```
(1) ローダ基盤          mod.json の "tool" 宣言 → gui.py がボタンを出し、
                        MOD 同梱の tool.py をサブプロセスで起動する（§4。実装済み）
(2) エクスポート (tool.py) 他世界のセーブを復号して NPC を一覧 → チェックした
                        NPCを1体1zipで state\npc_carryover\<元の世界名>\ へ（§5）
(3) インポート予約 (同上)  zip と置き先の世界名を選んで pending.json に書く（§5）
(4) 取り込み (apply)     予約された世界のロード時に、NPC をセーブへ入れて配置し、
                        記憶を state\ の新世界側へ写す（§6〜§7）
```

ゲーム内で完結させず GUI を外に出すのは、元の世界のセーブを読むのが
プレイ中のゲームと無関係な作業だから。ゲーム内 UI（`320_` の方式）だと
「どの世界の誰を」の指定が会話ボタンの語彙に収まらない。

## 3. エクスポートの形式（SaveEditor 互換の zip）

SaveEditor の NPC 移植（`Tools/NpcPortability.cs`、format = `instantale_npc`）と
同じ形式にする。エディタでエクスポートした zip をこの MOD の置き場に
置いても読めるし、逆も読める。

```
<名前>.zip
├─ npc.json          {"format": "instantale_npc", "version": 1,
│                     "source_world": 元の世界名, "original_id": 元のid,
│                     "original_name": 名前, "npc": {33項目のNPCレコード}}
├─ images/*.png      worlds\<元の世界>\characters\<名前>\ の画像
└─ carryover.json    この MOD だけが読む追加分。SaveEditor は
                     npc.json と images/ しか読まないので互換を壊さない
                     ・記憶（§7）
                     ・居た場所 `{"where": {"area": ..., "facility": ...}}`
```

- 置き場所は `state\npc_carryover\<元の世界名>\<名前>.zip`（`state\` の規約は TECH.md）
- エクスポート時に `display_position_in_battle` を null にする（エディタと同じ）。
  `relationship` / `life_log` は保持したまま出し、引き継ぐかは取り込み時に決める
- 名前はそのままファイルパスになる（GAME.md §2.15）ので、
  エディタの `SafeFileName` と同じ字を落としてから zip 名・画像フォルダ名に使う
  （`carryover.safe_name`。ローダの `state.world_filename` は使わない ―
  あちらは均した名前に短い印を足すので、ゲームの書いたフォルダ名と当たらなくなる）
- 書き出したときに滞在していた場所（エリア名・施設名）も同梱する。
  元の世界を消した後でも、zip だけで誰だったかが分かるように
- 画像を探す場所は `image_src` の記録から採るが、**そのまま使わない**。
  `image_src` は書いた機械の絶対パスなので、別の機械で作られた世界では
  他人のユーザー名を指したままこちらには存在しない。
  `worlds\` から後ろだけを残して `%LOCALAPPDATA%` に繋ぎ直す
  （`carryover.local_path`）

## 4. ローダ基盤: MOD が設定画面から自分の道具を開く

> **2026-08-29 に実装済み**（`322_battle_bgm` で先に入れた。TECH.md §3.12）。
> 違いは1つ: 「道具を開く」の別ボタンではなく、**「設定…」がその MOD では道具を開く**。
> `mod.json` の `"settings"` もその画面が引き受ける約束。以下は設計時の文のまま。

`gui.py` は「MOD のコードを一切 import しない」（`gui.py` 冒頭の原則）。
これを守ったまま MOD に GUI を持たせるため、宣言＋サブプロセスにする。

- `mod.json` に `"tool"` を足す:
  `{"entry": "tool.py", "label": {"ja": "...", "en": "..."}, "note": {...}}`
- `gui.py` は `tool` を宣言した MOD の行に「道具を開く」の操作を出し、
  `[sys.executable, <mod_dir>\tool.py]` をサブプロセスで起動する
- 渡すものは環境変数（引数だと tool 側の書式を縛る）:

| 変数 | 中身 |
| --- | --- |
| `IML_ROOT` | 配布フォルダの根（`runtime\` と `state\` の親） |
| `IML_STATE_DIR` | `ml.state_dir(RUNTIME_DIR)` の値 |
| `IML_GAME_DIR` | `settings\gui.json` が覚えているゲームの場所。未設定なら空 |
| `IML_MOD_SETTINGS` | `settings\mod_settings.json` のパス（tool が既定値の解決に使う） |

- tool 側は tkinter で自前の窓を持つ普通のスクリプト。ローダ本体は
  `sys.path` に在る（`runtime\` 直下）ので `instantale_modloader.state` の
  世界名の均し方をそのまま使える

## 5. エクスポートとインポート予約の GUI（tool.py）

##### 世界とNPCの一覧

- 世界一覧は `%LOCALAPPDATA%\Darmabeko\Instantale\saves\` のフォルダ名。
  **インストール先ではない**（`IML_GAME_DIR` の下に `saves` は無い。§9.1）
- `savedata.json` の復号は XOR（`cipher[i] = plain[i] ^ b"Instantale_Save_Key_2026"[i % 24]`。
  GAME.md §2.16）。読むだけで書き戻さないので、エディタが課している
  ラウンドトリップ検査は要らない
- NPC 一覧に出す列は 名前 / エリア / 施設 / 職（job） / 分類（category） / レベル
  （experience_level） / 親密度（`relationship` の player の affinity と
  affinity_text）。選んだ NPC の `face_image.png` はプレビューに出す
  （Tk がそのまま読めるので画像の部品は要らない。
  まだ絵の無い NPC ― 初回の会話まで生成されない ― は枠だけ出す）
- 顔の大きさは揃っていない。実データ194件のうち 144件が 165×165 で、
  **40件は 32×64 の小さい版**。枠に対して整数倍で伸ばし縮めする
  （Tk の `PhotoImage` は整数倍しか持たない）
- エリアと施設は**いま滞在している場所**。`current_area` / `current_location` を
  先に見て、欠けていれば `initial_location`（生成直後の個体はそちらにしか
  入っていないことがある）。実セーブのヴェスティアでは 103人全員が解決できた
- 1世界のNPCは100体を超えることがあるので、列ごとのフィルターを必須にする。
  名前は部分一致、エリア・施設・職・分類・親密度は選択、レベルは下限。
  欄は7つになるので2段に分ける（上が「誰か」、下が「どこに居るか・どれくらいの相手か」）。
  フィルター中は「何人中何人を表示しているか」と、いま効いている条件を一覧の下に出す
- 一覧の先頭はチェック列。チェックした全員を一括でエクスポートする
  （1人ずつのボタンは持たない。見出しのチェックで表示中の全員に入り切りできる）

##### 予約（pending.json）

`state\npc_carryover\pending.json` に1件1予約で持つ:

```json
{"zip": "<元の世界名>/<名前>.zip", "target_world": "置き先の世界名",
 "status": "pending",
 "inherit": {"memory": true, "relationship": true, "life_log": true}}
```

- 予約を消すボタンの文言は行の状態で変える。まだ置いていない予約は
  「予約を取り消す」、置いた後は「記録を消す」 ―
  **消えるのは控えだけで、世界に居る NPC は消えない**。
  同じ言葉で両方を指すと、置いた NPC を消すボタンに見える。確認は出さない
- `status` は `pending` → `placed`（+ 新id・配置先・日時） / `skipped`（+ 理由）。
  取り込み済みの判定はこのファイルだけで行い、セーブには独自キーを足さない
- `inherit` の既定値は MOD 設定（§8）から写す。予約ごとに変えられる
- 同じ zip を複数の世界へ予約するのは可（インポートは読むだけで zip を消さない）
- 選んだ zip の中身は右に出す（顔・名前・元の世界・滞在していた場所・職・分類・
  レベル・難易度・親密度・人物像・同梱物）。エクスポート側と同じものを zip から出す
- エクスポート済みのNPCの一覧もNPC一覧と同じフィルター付きの表にする
  （zip は溜まる一方なので数十〜百を超えうる）。列は 名前 / 元の世界 /
  職 / レベル / 親密度 で、元の世界は木の階層ではなく列＝フィルター条件として持つ

##### 予約時の名前検査

重複の検査はここが主役。置き先は世界名で指定するので、予約の時点で
置き先の `savedata.json` を読み、`npcs` の name と突き合わせられる。
ゲームを起動する前に分かれば手戻りが無い。

- 重複していたら予約の画面にその場で出し、そのままでは予約を確定させない
  （先住側をエディタで改名する・置き先を変える、のどちらかを選んでから）
- GUI を開き直したときも `pending` の全件を検査し直す。
  予約してから置き先の世界を遊ぶと、その間にゲームや `320_` が
  新しいNPCを作って衝突が生まれることがあるため。
  引っかかった予約には一覧で印を出す
- 判定はエディタの `NameExists` と同じ完全一致

## 6. ゲーム内の取り込み（apply）

発動は「予約された世界のロードが済んだとき」に1回。
世界名は `instantale_modloader.state.world_key(app)` で見分ける。
フックの地点は実機で確かめた（§9.2）。

手順（1予約ぶん）:

| # | やること | 根拠・注意 |
| --- | --- | --- |
| 1 | 名前衝突の最終弁。`npcs` の name と完全一致が居たら見送り、`status: skipped` にして通知（§6.1） | 検査の主役は予約時の GUI（§5）。ここに残すのは、予約からロードまでの間に生まれた衝突（プレイ中のNPC生成）を黙って通さないため。インポートするNPCは固有の人物なので勝手に改名しない |
| 2 | id の採番。**辞書の最大+1 ではなく `index.npc` で採番し、`index` を進める** | 最大+1 だとゲームが次に払い出す id と衝突して上書きされる（GAME.md §2.23。実セーブでも 97人の世界で台帳が 102 まで進んでいた）。ローダが既にそうしている（`ids.claim` / `make_npc` が台帳を進める）ので、こちらは何もしない |
| 3 | インベントリの item id を `index.item` で採番し直し、`equipments` の参照も付け替える | エディタの `ReassignItemIds` と同じ。inventory の鍵だけ変えて equipments を忘れると装備が宙に浮く |
| 4 | `current_area` / `current_location` / `initial_location` を落とし、`location` は全て null に戻す | 並び順は `make_npc` が守る（33項目を持つひな型への上書きなので位置が動かない）。場所は `make_npc` が置き先で埋める |
| 5 | `npcs.make_npc` で素データ書き込み・`Character` 組み立て・施設配置 | 採番も台帳の前進も `make_npc` の中。手を入れる必要は無かった |
| 6 | 配置先の決定: ダンジョン以外のエリアを1つ引き、**その土地のギルドか宿を半々で引く**。置ける施設が無ければ別のエリアを引き直す | 旅の者としてやって来る設定なので宿に居ても不自然ではない。土地 → 種別 → 施設 の順で引くのは、宿を2つ持つ土地があるため（実データ33件中2件）。施設から直に引くとそこだけ宿が2倍出る |
| 7 | そのエリアの `adventurer_npcs` へ id を登録（実行時とセーブ側の両方） | `320_` の `enroll` と同じ。旅の冒険者として一覧に出る |
| 8 | zip の画像を `worlds\<世界名>\characters\<名前>\` へ展開し、`image_src` の各値をそこへの**絶対パス**に貼り替える | エディタの `PlaceImages` / `RewriteImageSrc` と同じ。展開できなかった鍵は null にする（元の世界の絶対パスを残すと、その世界を消した瞬間に切れる） |
| 9 | 記憶を `state\` の新世界側へ写す（§7） | |
| 10 | `status: placed` を書き、ゲーム内メッセージで到着を知らせる | 「〈エリア名〉の〈場所名〉へ」 の形で出す。例:「「星読みのリリア」が旅の末に始まりの泥濘の灯り亭へ流れて来たらしい。」。場所は施設そのものの名前で、読めない個体のときだけ種別の語（「ギルド」「宿」）で埋める |

繰り返しロードしても増えない（`status` で1回きり）。
配置したNPCはゲーム自身の `npcs` 項目としてセーブに残り、
MOD を外しても消えない。`320_` と同じ性質で、消したいときはセーブエディタで消せる。

### 6.1 見送りの通知

ここに来るのは、予約時の検査（§5）を通った後にゲーム側で衝突が生まれた場合だけ。
稀だが、見送ったときに黙っていると「予約したのに出てこない」がバグに見える。

- その世界のロード直後に1回、ゲーム内メッセージで理由を出す
  （例:「〈名前〉はこの世界に同名の人物が居るため現れなかった。」）
- 予約は `skipped` として残す。手で対処（エディタで先住側を改名するなど）して
  `pending` に戻せば次のロードで再試行される
- 2回目以降のロードでは通知せず、`out\npc_carryover.log` にだけ書く

## 7. 記憶の引き継ぎ

`311_`（人物像・あなたについての記録・判明した事実）と `403_`（社会関係）は
セーブではなく `state\npc_profiles\<世界名>.json` / `state\npc_social_memory\<世界名>.json` に
NPC id を鍵として入っている。世界も id も変わるので、両方を付け替えて写す。

- エクスポート時に、元の世界の該当 id の項を `carryover.json` へ同梱する。
  zip を自己完結にするためで、後から元の世界の `state\` が消えても効く
- 取り込み時（`inherit.memory` が真のとき）、置き先の世界のファイルへ新 id で書く。
  書き方は `311_` と同じ「隣に作ってから差し替え」
- `403_` の社会関係は相手の id が並ぶ形なので、そのまま写しても相手が居ない。
  同じ世界へインポートしたNPC同士の間だけ id を付け替えて残し、それ以外の相手の項は落とす
- `relationship` / `life_log`（セーブ内の項目）は `inherit` の指定に従い、
  引き継がない場合はエディタの既定値と同じ形（player のみ・好感度0・初対面）に戻す

## 8. 設定（mod.json の settings）

| 設定 | 既定 | 意味 |
| --- | --- | --- |
| 記憶を引き継ぐ | ON | `311_` / `403_` の記録を新世界へ写す。予約ごとの `inherit.memory` の既定値 |
| 好感度・関係を引き継ぐ | ON | セーブ内 `relationship`。切ると初対面から |
| 経歴を引き継ぐ | ON | セーブ内 `life_log`。切ると空でインポートする |
| 到着を知らせる | ON | 現れた場所と、見送ったときの理由をゲーム内の文で出す（§6.1） |

素のゲームに無い機能なので「素の値」という基準は無いが、
番人値は使わず ON/OFF で持つ（settings の規約どおり）。
3つの引き継ぎは**予約するときの既定値**で、予約ごとに変えられる（§5）。
画面で切り替えた値はそのまま設定へ戻る（設定の入口を2つ持たないため）。

## 9. 確かめたことと、まだのこと

### 9.1 実セーブで確かめた（2026-08-30。ゲームは起動していない）

| 分かったこと | どこで効くか |
| --- | --- |
| セーブは `%LOCALAPPDATA%\Darmabeko\Instantale\saves\<世界>\savedata.json`。**インストール先には無い**（`IML_GAME_DIR` は使えない） | `carryover.data_dir` |
| 立ち絵は `worlds\<世界>\characters\<名前>\`。`image_src` は**絶対パス** | 画像の持ち出しと貼り替え（§6 手順8） |
| `image_src` の絶対パスは**書いた機械のもの**。別の機械で作られた世界では他人のユーザー名を指す（ペルディションは 95人中 93人が `C:\Users\Owner\...` で、その版は顔をほぼ全部取り落としていた） | `carryover.local_path` が `worlds` から後ろを残して `%LOCALAPPDATA%` へ繋ぎ直す |
| 手元の5世界とも XOR。素の JSON では読めない（フォールバックは残す） | `carryover.decode` |
| NPC の項目は 33項目でこの順。32項目の個体は `speech_style` だけが無い（ヴェスティア 103人中 11人） | `NEW_NPC_TEMPLATE` に合わせて渡せば並びは崩れない |
| `affinity_text` は**文字列のことも配列のこともある**（ヴェスティア 103人で str 57 / list 46） | 一覧の親密度とフィルター（`affinity_of`） |
| `relationship` の相手は `player` だけ（103/103） | 引き継ぎで写すのは player の項だけでよい |
| `index['npc']` は実在の最大+1 とは限らない（ドスケベフェスティバルは 97人で台帳 102） | 採番は `ids.claim`。§9.3 の1件目は解決済み |
| エリアの `size` は `town` / `village` / `city` / `dungeon`。ヴェスティアは 54個中 45個がダンジョン | 置き先の選び方（§6 手順6） |
| 非ダンジョンのエリアは**施設が在るなら必ずギルドと宿の両方を持つ**（5世界45件のうち施設が在る33件が全件そう）。残る12件は施設が0件で、まだ生成されていない（初訪問で作られる） | 置き先の選び方（§6 手順6）。施設0件のエリアは候補から外れる |
| 持ち物の鍵は `item_<n>`、`index['item']` で採番 | `reassign_items`（§6 手順3） |

### 9.2 実機（2026-08-30。7回）

ヴェスティアの「ジョゼット」を 新テストワールド へ持ち込んだ。
**4回目で通った**（セーブに残り、名簿にも載った）。
そこまでに不具合が3つ出て、どれも直してある。
その後「オーガスト」（記憶 OFF）・「テラ」（記憶 ON）・「コーウィン」（記憶 ON）を
1体ずつ持ち込み、どれも1回で通って保存まで届いた。
4体で id 43〜46、`index['npc']` は 43 から 47 まで進んだ。

##### フック地点は確かめられた

`224_` の記録（`out\npc_carryover_probe.log`）:

| 地点 | 結果 |
| --- | --- |
| 注入時（`on_ready`） | NOT READY（世界がまだ無い） |
| `load_game_new` の前 | NOT READY |
| **`load_game_new` から戻った直後** | **READY**（world_key=新テストワールド / index.npc=44 / エリア21・ギルド4・宿4） |

いま包んでいる地点で正しい。

##### 不具合1: ロード直後は素データの辞書を見分けられない

`make_npc` のログが `npc stores = <none>` を出し、
`generate_character(43)` が `KeyError: '43'` で落ちて、
最後の手段（`Character` の直組み）に落ちていた。

原因はローダの `npcs.npc_stores`。
素データの辞書を「**実行時の名簿（`world.characters`）と鍵が重なるか**」で
見分けていたが、ロード直後の名簿はまだ空
（`224_` の記録では**1件**）。突き合わせる相手が居ないので何も見つからない。

結果、`save_data_dict['npcs']`（GAME.md §2.23 が「★ ここが本体」と書いている辞書）へ
何も書かれず、**セーブに残らなかった**
（実際 `savedata.json` の 新テストワールド は 43人のままで id 43 は無い）。

直し方: 中身の形でも見分ける（値が `name` と `ability_scores` を持つ辞書）。
`ability_scores` の6鍵は `generate_character` が要求するものなので、
「素データである」ことの必要条件そのもの。
ローダ側の修正なので `320_` / `902_` にも同じ効き方をする
（あちらも同じ場面では黙って何も書けていなかった）。

##### 不具合2: 注入し直しで言付けが消える

`out\modloader.log` を見ると、ロード（14:03:37）の後・画面が出る前に
**注入し直し**（14:03:39）が走っていた。
到着の知らせは「ロードの後・最初に選択肢が組み直されるまで」持ち越すものなので、
閉包に置いていると新しい世代の空のリストが読まれて消える（TECH.md §3.5）。

直し方: プロセスに置く（`sys` の属性）。

##### 不具合3: 置いても、プレイヤーが保存しなければ残らない

不具合1を直した3回目（14:13:07）は、素データが両方の辞書に入った。

    make_npc: npc stores = [("app.world_dict['npcs']", 44), ("app.save_data_dict['npcs']", 44)]

`generate_character` の `KeyError` も消えた。
それでも `savedata.json` は 43人のままだった ―
**ロードしてそのまま終えたので、ゲームが保存していない**。

この MOD が触るのは動いているゲームの中の辞書で、ファイルが書かれるのは
プレイヤーが保存したとき。
予約を果たした瞬間に `placed` を書いてしまうと、
保存せずに終えた回で予約だけが消費される。

直し方: 保存されたかを別に控える（`saved`）。

| 予約の状態 | 次のロードで |
|---|---|
| `placed` ＋ `saved` 有り | 触らない（後で消したのはプレイヤーの意思） |
| `placed` ＋ `saved` 無し ＋ その NPC が世界に居ない | `pending` に戻して置き直す |
| `placed` ＋ `saved` 無し ＋ 世界に居る | そのまま（同じ回の続き） |

印を付けるのは `InstantaleApp.save_game` を包んだところ。

##### 通った（4回目・保存あり）

不具合1を直した回でロードし、そのまま遊んで保存したセーブ
（`savedata.json`。2026-08-30 14:20:35）を読み直した:

| 見たところ | 結果 |
| --- | --- |
| `npcs` の件数 / `index['npc']` | 43 → **44** / 43 → **44** |
| id 43 | **ジョゼット** |
| 項目の並び | 33項目・既定どおり（セーブエディタの表示は崩れない） |
| 置き先 | area 0（はじまりの丘）/ facility 7 のギルド |
| `adventurer_npcs` | `['8', '9', '10', '43']` |
| 立ち絵 | `worlds\新テストワールド\characters\ジョゼット\` に6枚。`image_src` の全部が実在 |
| 持ち物 | `item_16`（置き先の台帳で採り直した番号） |
| 元の世界 | ヴェスティアのジョゼットはそのまま残っている（写しであって移動ではない） |

**取り込みの経路そのものは決着**。
**立ち絵と会話も確認済み**（2026-08-30）。
はじまりの丘のギルドの一覧に出て、話せて、貼り替えた絶対パスの絵が表示された。
`adventurer_npcs` へ載せる経路（`320_` と同じ）はギルドについては足りている。

残るのは `403_` の社会関係（§9.4）。

##### 記憶を持ったまま持ち込んだ（6回目。2026-08-30 16:12）

ヴェスティアの「テラ」を、引き継ぎを3つとも ON（`memory` / `relationship` /
`life_log`）にして 新テストワールド へ持ち込んだ。**決着**。

| 見たところ | 結果 |
| --- | --- |
| 取り込み | `テラ: placed as 45 at area 6 / facility 82 (guild)`。番人の警告は出ていない |
| 保存 | `saved: テラ now live in 新テストワールド`（16:12:41） |
| 記憶を書いた | `memory: carried npc_profiles for 45`。`state\npc_profiles\新テストワールド.json` に `"45"` が増えた |
| 写した中身 | ヴェスティア側の項そのまま（`updated` は元の世界の 2026-08-12 のまま。書き直していない） |
| **読み戻した** | 会話を始めたとき `311_` が載せた。`starter: 'テラ' (45) +2426 chars into profile`（`out\npc_profile.log` 16:42:01） |

**id を跨いだ引き継ぎが両側で成立している。**
書く側（この MOD が新しい id で `state\` へ写す）と
読む側（`311_` がその id で引く）が繋がったことを、別々のログの2行が示している。

`403_` の社会関係は今回も写していない（ログの `carried` は `npc_profiles` だけ）。
元の世界に `state\npc_social_memory\ヴェスティア.json` が無く、写すものが無かったため。
**経路が動くことはまだ示せていない**（§9.4 の 1）。

##### もう1体、同じ形で通った（7回目。2026-08-30 16:57）

ヴェスティアの「コーウィン」を引き継ぎ3つとも ON で持ち込んだ。
テラと同じ行が同じ順で出た（`placed as 46 at area 5 / facility 56 (guild)`・
`memory: carried npc_profiles for 46`・`saved: コーウィン now live in 新テストワールド`）。
置き先はテラと別のエリア・別のギルドで、引き当ては固定されていない。

##### 確認が済んだので4体を戻した（2026-08-30 18:50）

7回ぶんの確認が終わったところで、`新テストワールド` を持ち込み前の状態へ戻した。
いまの `saves\新テストワールド\savedata.json` は `npcs` 43件・`index['npc']` 43。
戻す前の版は
`saves\新テストワールド\backups\ミツバ_新テストワールド_savedata_20260830_185037.zip` に
47件・`index['npc']` 47 で残っていて、id 43〜46 が ジョゼット / オーガスト / テラ / コーウィン。

次に §9.4 の残りを測るときに要ること:

- `state\npc_carryover\pending.json` は空。`placed` の記録も消えているので、予約から取り直す
- `worlds\新テストワールド\characters\` には4体ぶんの立ち絵が残ったまま。
  同じ名前で持ち込み直すと、その上へ展開する（§6 手順8）
- 持ち込んだ状態から続けたいなら backups の zip の `savedata.json` を戻す

##### 足した番人

`make_npc` の後に、その id が `save_data_dict['npcs']` に入ったかを確かめて、
入っていなければログと `modloader.log` の両方に警告を出す。
黙って通すと、次にゲームを開いたとき居ないことで初めて気づく。

### 9.3 ゲーム抜きで通した（`tools/tests/test_wip_npc_carryover.py`）

復号・名前の消毒・zip の往復（33項目の並び・画像・記憶）・予約の読み書き・
フィルター・取り込み（世界に入る／`adventurer_npcs` に載る／持ち物の採り直し／
記憶を写す／2度目のロードで増えない）・見送りと通知・滞在先の引き当て・
別の機械で作られた世界の立ち絵・ロード直後の素データの見分け・
注入し直しを跨ぐ言付け・保存の確認と置き直し・到着の知らせの書き方・置き先の引き方。84項目。

### 9.4 まだ実機で見ていない（リリース時に VERIFICATION.md へ）

| # | 確かめること | 手順 |
| --- | --- | --- |
| 1 | `403_` の社会関係を写す経路 | 記憶（`311_`）は通った（§9.2）。**元の世界に控えがまだ1つも無い**: `state\npc_social_memory\` に在るのは `テストワールド.json` だけで、ヴェスティアにも新テストワールドにも無い。`403_` が控えを書くのは NPC が2人以上参加した会話のときだけ（`out\npc_social_memory.log` の `extract skipped: fewer than two NPC participants`）。先に ヴェスティア で仲間を連れた会話を1回起こして控えを作り、その後で持ち込む |
| 2 | `120_` が持ち込んだ NPC を重複改名の対象にしないか | **4体とも改名されなかった**。`120_` は `World.generate_character` を包んでいて、`make_npc` はそこを通る（§6 手順5）ので、持ち込んだ4体も `120_` の判定を受けている。それでも `out\npc_name.log` の最終行は 2026-08-29 21:19 のままで、2026-08-30 の4体は1行も増やしていない。残るのは衝突する組を引くこと。完全一致は §6 手順1 が見送るので、`120_` の部分一致（`'泥濘のガラム'` が `'荒ぶる戦士・ガラム'` に当たる形）を見るには、**先住の名前を含む名前の zip** を1本わざと持ち込む |
| 3 | 装備を持ったまま持ち込んだときの挙動 | **まだ引けていない**。書き出し済みの5本はどれも `equipments` が空で、持ち物も ジョゼット の1点だけ。GUI で装備持ちを選んで書き出すところから。`402_` が直した渡すクラッシュと同型の穴が無いかを見る |
| 4 | 1体あたりの zip の大きさ | **実測した**（2026-08-30。5本）。1本 2.09〜2.41MB（画像6〜7枚ぶん）、5本で 11.32MB。以前ここに書いていた 3.4MB は誤り。100体書き出しても `state\` は 250MB 前後 |
| 5 | 宿（`inn`）に置いた NPC が会話の一覧に出るか | 置き先をギルドと宿の半々にしたのは 2026-08-30 の GUI 直しと同じ回で、**まだ実機を通していない**（それまでの5体は全部ギルド）。何度か持ち込めば半分は宿になる。ギルドと同じく `adventurer_npcs` に載せているだけなので、一覧に出るかは宿で別に見る |

> 置き先は**ギルドと宿を半々**で引く。
> ギルドを優先していた版は宿が一度も選ばれなかった ―
> 生成されたエリアは必ず両方を持つため（§9.1）、常にギルドで確定していた。
> 旅の者が宿に居るのは自然なので、優先をやめて引き分けにした。

## 10. 段取り（残り）

1. ~~probe~~ / ~~ローダ基盤~~ / ~~tool.py~~ / ~~取り込み~~ / ~~記憶~~ / ~~見送りと通知~~ — 書けている
2. ~~実機で確かめる~~ 済（§9.2。4回目で通った）
3. ~~会える・絵が出る~~ 済。~~記憶の引き継ぎを ON にした予約で1体~~ 済（§9.2 の6回目）
4. 正式な番号（`3xx`）へ振り直し、`load_order.json` と `docs/` の7冊へ戻す
