# `104_balance_area_bgm`: 新しいエリアのBGMの偏りを均す

新しく生成されるエリアの曲が一部に偏るのを均す。
使われていなかった曲が出るようになる。

| 設定 | 意味 |
|---|---|
| 曲が無いエリアにも割り当てる | 既定 ON |

既存セーブの BGM をまとめて均したいときは `tools\rebalance_saved_bgm.py`（既定は
dry-run、実行時は自動バックアップ）。
