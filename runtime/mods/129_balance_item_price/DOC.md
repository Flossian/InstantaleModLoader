# `901_balance_item_price`: アイテムの値付け（分離した記録）

この MOD は**開発中（9xx）**。git には入れるが、CI・配布物・`load_order.json`・
`docs\` の5冊には入らない（TECH.md §2.6）。利用者向けの説明はここに置いてある。

| ここにある節 | 戻す先 |
|---|---|
| 遊び方・設定・困ったとき | `docs\MODS.md`（`1xx` の並びの正式な番号の位置） |

検証の記録は `docs/VERIFICATION.md` §3.19 に残してある（実機で確かめるときの
手順として使うので、あちらは移していない）。リリースで正式な番号へ振り直すとき、
下の見出しごと `MODS.md` へ移し、フォルダ名の番号を直して、このファイルを消す。

---

### `901_balance_item_price`: アイテムの値段を付け直す

素のゲームの値付けは**レア度をほとんど見ていない**。同じ価値段階なら common でも
mythic でもほぼ同じ額になり、店への売値はさらに低い。実セーブから拾った例:

| 品 | 素のゲーム |
|---|---|
| mythic の財宝（価値段階 66） | 売価 **102G** |
| epic の魔法素材（価値段階 4） | 売価 **3G** |
| mythic の遺物（価値段階 12） | 売価 **7G** |
| magical の短剣（攻撃力 245） | 買価 1,831G |

宿の一番安い部屋（簡易寝台）が 10G、一番高い部屋（高級個室）が 1000G ―
どちらも3ヶ月ぶんの前払い ― という物価なので、伝説級の戦利品を売っても
高級個室ひと部屋にも届かない。拾った品を売る意味がなく、店に並ぶ品を買う理由も
薄くなる。

この MOD は値段を**その品自身から組み直す**。元の額に倍率を掛けるのではないので、
効いていなかったレア度もここで効くようになる。

```
値段 = 種別ごとの基準額 × レア度倍率 × 種別の倍率 × スキル/強化 × 全体倍率
```

- 能力値を持つ品（武器の攻撃力・防具の防御力・薬の回復量）は**能力値**を軸にする。
  同じ段階でも強い品のほうが高い
- 能力値を持たない品（素材・財宝・道具・書物）は**価値段階**を軸にする
- 店に売るときの値段は、店が売る値段の 4 割（既定）

既定でどのくらいになるか:

| 品 | 素のゲーム | この MOD |
|---|---|---|
| 短剣 common（攻撃力 23） | 72 | 403 |
| 短剣 common（攻撃力 96） | 463 | 2,980 |
| 短剣 magical（攻撃力 245） | 1,831 | 22,100 |
| 革鎧 common（防御力 227） | 1,833 | 8,950 |
| 薬草 common（回復 8） | 14 | 52 |
| 薬 magical（回復 198） | 353 | 2,970 |
| 魔法素材 magical（段階 24）買 | 128 | 810 |
| 魔法素材 magical（段階 24）売 | 20 | 324 |
| 遺物 mythic（段階 12）売 | 7 | 1,280 |
| 財宝 mythic（段階 66）売 | 102 | 14,900 |

**素のゲームより安くなる品は無い。**

| 設定 | 意味 |
|---|---|
| 全体の倍率 | 既定 1.0。値段すべてに最後に掛かる。高いと感じたら 0.5、安いと感じたら 2.0 |
| 店の買取率 | 既定 0.4。店に売るときの値段は、店が売る値段のこの割合 |
| 武器 / 防具・装身具 / 回復アイテム / 消耗品 / 道具・書物 / 素材・財宝 の倍率 | 既定 1.0。種別ごとに上げ下げする |
| レア度 rare / magical / epic / legendary / mythic の倍率 | 既定 1.4 / 2.0 / 3.0 / 4.5 / 7.0（common は常に 1.0） |
| 強化1段あたりの上乗せ | 既定 0.3。鍛冶で強化した装備に段数ぶん足す |
| スキル付きの倍率 | 既定 1.5 |
| 値段の下限 / 上限 | 既定 5 / 9,999,999 |
| 手持ちの品も開いたときに付け直す | 既定 ON。切ると、これから作られる品だけが新しい値段になる |
| 売買した金額を表示どおりに合わせる | 既定 ON。下の「表示と決済がずれたら」を参照 |

細分ごとの基準額（短剣・宝石・財宝…の32種）は GUI ではなく
`runtime\mods\901_balance_item_price\balance_item_price.py` の `RATES` にある。
「宝石だけもっと高く」のような調整はそこを直す。

#### 古いセーブの品

値段はアイテム自身に書かれてセーブに残る。この MOD は**売買画面を開いた時点**と
**説明欄に出した時点**で付け直すので、遊んでいる途中の世界でも次に店へ行けば
新しい値段になる。逆に、この MOD を外しても付け直した値段はそのまま残る
（ゲームが値付けし直す品から順に元へ戻る）。

#### 表示と決済がずれたら

ゲームが表示どおりの額で決済しているかは、外から確かめられない。この MOD は
売買の前後で所持金を測り、**動いた額が表示と違えば、その差だけ所持金を直す**。
所持金が負になることはない。直した回は `out\item_price.log` に `reconcile` として
残る。この行が1つも出ないなら、ゲームは表示どおりに決済している。

#### 困ったとき

| 症状 | 見るところ |
|---|---|
| 値段が変わらない | `out\item_price.log`。`installed` の行だけで `->` の行が無ければ、まだ値付けの経路を通っていない（店を開くか、アイテムにカーソルを合わせる） |
| 高すぎる・安すぎる | 「全体の倍率」を動かす。種別ごとなら種別の倍率 |
| 特定の品だけおかしい | 同じログの `[...]` が値段の軸（`攻撃力=96` か `value=24`）。`skip` が出ていれば、その品は価値段階も能力値も読めていない |
| `reconcile` が大量に出る | ゲームは表示と違う額で決済していて、この MOD が毎回直している。動作としては正しいが、この行を添えて報告してほしい |
| 元の値段に戻したい | この MOD を切ってから、店を開いて品揃えが入れ替わるのを待つ（`312_shop_restock` を併用していれば入れ替えの日で戻る） |
