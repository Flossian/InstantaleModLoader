# `221_probe_item_level`

品物のレベルを誰が決めているかを録る。土地の依頼の難易度を返す4関数と、呼び出し側が `tier` に渡している実値。難易度→値段と値段→レベルの変換6本。店の品揃えの生成に渡る段と、生成後に主が持っている品の `value`。そしてクラフトの経路（`ItemCraftManager.calculate_modification` の実引数と戻り値）。1回目の計測で鎖は端まで繋がった（GAME.md §2.13.1.2 / §2.14.2）ので、残るのは `tier` と倍率の**式**を出すための通り数集め
