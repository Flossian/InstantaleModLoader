# `107_fix_battle_flag_stuck`: ロードすると戦闘BGMで始まるのを直す

会話からの戦闘で「戦闘中」の印が下ろされず、セーブに焼かれるのを修正。
一度戦闘すると施設でNPCから話しかけられなくなるのも同じ原因だった。

> **main_024 で本体が取り込んだため無効化**。
> デバッグモードのときだけ読み込まれます（README「デバッグモード」）。
> 「戦闘中」の印が残らなくなったので、この MOD は要りません。

## 困ったとき

| 症状 | やること |
| --- | --- |
| ロードすると戦闘BGMで始まる | 入れ直す。読み込んだ時点で直る |
| 一度戦闘したらNPCから話しかけられなくなった | 同上（同じ原因） |
